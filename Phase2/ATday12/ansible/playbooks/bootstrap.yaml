- name: Bootstrap Python on fresh servers
  hosts: all
  become: true
  gather_facts: false

  tasks:
    - name: Install Python and required libs
      raw: |
        apt-get update -y &&
        apt-get install -y python3 python3-apt python3-six

    
